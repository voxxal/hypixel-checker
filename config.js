/**
 *
 * hypixel-checker
 *
 * Checks if anyone has logged into your account and notifies you
 *
 * Github Repository:
 *
 *
 * Change config by entering your username and api key 👇
 **/
const CONFIG = {
  USERNAME: "USERNAME", //Minecraft username
  API_KEY: "API_KEY", //Api key, get it by typing /api in hypixel
  CHECK_FREQUENCY: 5, //How often it checks (In seconds) Must be greater than 0.5
  NOTIFICATIONS: true, //Whether or not to send notifications
};
